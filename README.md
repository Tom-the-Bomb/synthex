# Synthex

An interactive K-map & boolean algebra minimizer & visualizer:
truth table, grouped K-map, minimal SOP/POS, and a 3D torus view.

Built with `React.js`, `Tailwind`, `Three.js`, and `Katex`
